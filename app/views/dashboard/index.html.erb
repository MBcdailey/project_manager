<nav class="navbar justify-content-end navbar-dark bg-dark">
  <form>
  </form>
</nav>
<div class="row header-row px-3 py-3 mb-5">
  <div class="col-12">
    <h1>Dashboard</h1>
  </div>
</div>
<div class="row panel-row px3 py-3">
  <div class="col-6">
  <div class="panel-column col-11 offset-1 shadow-light">
    <!-- RECENT PROJECTS -->
    <div class="row header-row px-3 py-3 bg-dark">
      <div class="col-12">
        <h2>Recent Projects</h2>
      </div>
    </div>
    <% @projects.each do |project| %>
    <div class="row project-row px-3 py-3">
      <div class="col-12 py-2 border-bottom">
        <h3><a href="<%= project_path(project.id)%>"><%= project.title %></a></h3>
        <p><%= project.description %></p>
        <p>Created: <%= format_date_time(project.created_at) %></p>
      </div>
    </div>
    <% end %>
  </div>
  </div>
  <div class="col-6">
  <div class=" panel-column col-11 shadow-light">
    <!-- PANEL CATEGORY -->
    <div class="row header-row px-3 py-3 bg-dark">
      <div class="col-12">
        <h2>Panel Category</h2>
      </div>
    </div>
    <div class="row project-row px-3 py-3">
      <div class="col-12 py-2 border-bottom">
       <p>..</p>
      </div>
    </div>
  </div>
  </div>
</div>
